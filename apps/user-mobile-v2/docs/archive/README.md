# Archive — apps/user-mobile-v2/docs

## `build_error_android.md` (792 KB) and `error_android.md` (90 KB)

Raw Android EAS/Gradle build-error output captured 2026-06-15 while debugging the Android native build. That's the same day `../04-sprints/SPRINT_23_IOS_PWA_ANDROID_BUILD_PLAN.md` was created, which documents the Android build working (preview APK installed and running on emulator, Supabase connecting) — so the errors captured in these two logs were resolved same-day.

**Why archived:** these are raw debug-session dumps, not reference documentation — nothing in the current build process needs them. Kept only in case a similar Gradle/EAS error resurfaces and the historical resolution is useful. If the Android build is stable through a full production release, these can be deleted entirely rather than kept.
