# V1 Parity Lock

Canonical document:

`docs/USER_MOBILE_V2_V1_PARITY_LOCK.md`

This v2 app must follow the root parity lock before continuing feature sprints.

Short rule:

- UI/UX pass is allowed, but must be labelled UI/UX only.
- Dummy/sample flows must not be presented as real production function.
- Sprint sign-off requires end-to-end v1 behavior or an explicit documented boundary.
