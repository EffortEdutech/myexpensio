{
  "version": "4.3.2",
  "changelog": [
    "support Bearer token auth in exports endpoint for mobile ()",
    "support Bearer token auth in all mobile-facing API routes ()",
    "accept Bearer token auth in tng/parse for mobile ()"
  ]
}
