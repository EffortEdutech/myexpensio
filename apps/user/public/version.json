{
  "version": "4.4.3",
  "changelog": [
    "mobile PDF includes TNG statements with highlight via scan service ()",
    "remove duplicate DEFAULT_PDF_LAYOUT and PdfLayoutConfig in exports route ()",
    "mobile PDF fast path — use local payload, skip Supabase claims query ()",
    "replace createClient type ref with SupabaseClient in push route ()"
  ]
}
