# apps/user/.env.local additions — AI-Assisted Capture
# Copy this line into your .env.local

# ── Gemini API (AI Capture — receipt/odometer/voice) ──────────────────────────
# Get a free key (no card required, Malaysia is a supported region): https://aistudio.google.com/apikey
# Server-only — never expose to the client. Same handling as SCAN_API_SECRET.
GEMINI_API_KEY=your-gemini-api-key
