name: Validate

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: build-${{ matrix.app }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app:
          - user
          - admin
          - cs
    env:
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: test-anon-key
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: test-anon-key
      SUPABASE_SERVICE_ROLE_KEY: test-service-role-key
      NEXT_PUBLIC_APP_VERSION: ci
      NEXT_PUBLIC_APP_URL: http://localhost:3100
      GMAIL_SMTP_USER: ci@example.com
      GMAIL_SMTP_APP_PASSWORD: dummy-password
      MAIL_FROM_NAME: myexpensio CI
      STRIPE_SECRET_KEY: sk_test_dummy
      STRIPE_WEBHOOK_SECRET: whsec_dummy
      TOYYIBPAY_SECRET_KEY: dummy-secret
      TOYYIBPAY_CATEGORY_CODE: dummy-category
      TOYYIBPAY_BASE_URL: https://dev.toyyibpay.com
      TOYYIBPAY_CALLBACK_URL: http://localhost:3101/api/admin/webhooks/billing/toyyibpay
      NEXT_PUBLIC_CS_APP_URL: http://localhost:3102
      NEXT_PUBLIC_USER_APP_URL: http://localhost:3100
      NEXT_PUBLIC_WORKSPACE_APP_URL: http://localhost:3101
      CS_APP_LOGIN_URL: http://localhost:3102/login
      USER_APP_LOGIN_URL: http://localhost:3100/login
      WORKSPACE_APP_LOGIN_URL: http://localhost:3101/login

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build
        run: pnpm build:${{ matrix.app }}
