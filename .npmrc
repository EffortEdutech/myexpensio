# Hoist all packages to flat node_modules.
# Required so Gradle's require.resolve() calls in settings.gradle can locate
# react-native, expo, and related packages during the Android build.
node-linker=hoisted
shamefully-hoist=true
